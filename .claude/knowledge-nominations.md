# Knowledge Nominations

Candidate learnings from agents and sessions. The auditor reviews these
during each audit cycle and promotes valid ones to knowledge-base.md.

## Pending Nominations

<!-- All 6 nominations from 042626 reviewed by auditor 042626. 5 promoted to knowledge-base.md. 1 deferred (see below). -->

## Deferred

- [042626 — DEFERRED] /wrap-up: W3C annotation anchoring (prefix+text+suffix, ~30 chars) survives marked.js re-rendering. `Range.surroundContents` fails on cross-element ranges; `extractContents`+`insertNode` fallback handles them. | Reason deferred: project-specific implementation detail, not broadly applicable across projects. Retained here for future Liseuse-specific SOP if annotation bugs recur. | Evidence: highlight survived full page reload + re-anchor.
