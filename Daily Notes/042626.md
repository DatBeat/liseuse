# 042626 - Daily Work Log

## Decisions
- Onboarding Claudify terminé
- Stack confirmée: GitHub + Vercel + Supabase + Klaviyo
- Split mono-fichier `liseuse.html` autorisé (HTML + CSS + JS modules)
- Roadmap features: multi-fichiers → bibliothèque → sauvegarde → export PDF → édition → recherche
- Annotations: anchoring W3C TextQuoteSelector (prefix+text+suffix, 30 chars)
- Export PDF: print CSS (`@media print`) plutôt que jsPDF/server-side
- Architecture: ES modules natifs (pas de build tool) + IndexedDB local-first

## Meetings & Conversations
- Setup interview avec Claude Code: objectifs produit, workflow, pain points, options Tier 1/2/3 v2

## Notes
- Projet `liseuse.html` mono-fichier 1239 lignes initialement, dépendances CDN (marked.js 11.1.1, highlight.js 11.9.0)
- Pas de git init au départ, pas de package.json
- Design soigné déjà — accent terracotta papier, dual theme, polices Fraunces/Source Serif 4/Inter Tight/JetBrains Mono
- 11 commits sur main aujourd'hui
- 9 modules ES créés sous `src/` (storage, reader, library, stats, annotations, focus, editor, print, frontmatter)
- Tests live via Claude in Chrome: chaque feature validée navigateur réel sur prod Vercel

## End of Day Summary

### Accomplissements (Liseuse v1 → v2 production-ready)
1. **Infrastructure:** git init, repo GitHub public `DatBeat/liseuse`, projet Vercel `datbeatbusiness/liseuse`, alias stable `liseuse-datbeat.vercel.app`
2. **Architecture:** mono-fichier 1239 lignes → 9 modules ES + index.html + styles.css + manifest.json + sw.js + icons/
3. **v2 Tier 1 (habit foundation):**
   - Bibliothèque IndexedDB + drawer + recherche + auto-restore
   - PWA installable + Service Worker offline + file_handlers `.md`
   - Stats tracking (5s tick, 60s idle) + badge topbar + modal hero+chart+totals + streak
   - Annotations 3 couleurs + notes (W3C prefix/suffix anchoring, persistance cross-session)
   - Mode focus (paragraphe central highlight, autres dimmed, Cmd+Shift+F)
4. **v2 Tier 2 (différenciation):**
   - Front matter YAML cover (eyebrow/title/subtitle/author/date/tags pills)
   - Éditeur inline drawer avec textarea mono + Cmd+S save back to IDB
   - Print/PDF via @media print CSS (chrome masqué, page-break, links auto-URL)
5. **Automation:** scheduled remote agent pour 2026-05-03 → health check + draft schéma Supabase

### Décisions clés
- Pas de build tool → KISS, lecture/édition directe modules
- Local-first IndexedDB avant cloud sync (Supabase déféré)
- Édition inline = drawer textarea raw MD plutôt que contenteditable
- Stats: 1 min/jour de lecture active = jour éligible streak

### Open items reportés à demain
- **CRITIQUE:** désactiver Vercel SSO Authentication dans dashboard (sinon `liseuse-datbeat.vercel.app` reste 401 et les utilisateurs perdent leurs IDB à chaque deploy)
- Vérifier alias stable répond 200 sans auth post-fix
- Tester gros fichiers MD (>500 ko)

### Tomorrow's priorities
1. Désactiver Vercel SSO + vérifier alias stable public
2. Tester perf parsing gros fichiers MD
3. (3 mai) Review PR Supabase schema de l'agent planifié

### Knowledge gained
5 nominations promues en knowledge-base.md par l'auditor:
- Vercel deploy URLs = origins distincts (IDB ne traverse pas)
- Vercel SSO = dashboard-only (CLI ne peut pas)
- ES modules nécessitent serveur HTTP (pas file://)
- User préfère exécution autonome (pas de prompts mid-plan)
- Ne jamais persister hashed Vercel URL — utiliser alias stable ou `vercel ls`
